
# 🤖 ClaudeAutoAgent

A simple **agentic AI application** built using **Microsoft AutoGen** and **Anthropic Claude** (Claude 3.5 / Opus / Haiku / Sonnet compatible).
This project demonstrates how to create a **Claude-powered autonomous agent** that can process tasks, reason, and return structured responses — with clean, minimal, and extendable code.

---

## ✨ Features

* **Autonomous agent reasoning** using Microsoft AutoGen
* **Claude-powered inference** via Anthropic API
* Suppresses unwanted model cost warnings for clean output
* Well-structured and easy to modify
* Extensible into **multi-agent orchestration workflows**

---

## 🛠️ Tech Stack

| Component         | Purpose                       |
| ----------------- | ----------------------------- |
| **Python 3.10+**  | Development language          |
| **autogen**       | Agent orchestration framework |
| **anthropic**     | Claude model API client       |
| **python-dotenv** | Secure API key loading (.env) |

---

## 📦 Setup Instructions

### 1️⃣ Clone the Project

```bash
git clone <your-repo-url>
cd ClaudeAutoAgent
```

### 2️⃣ (Optional) Create a Virtual Environment

```bash
python -m venv venv
source venv/bin/activate      # Mac/Linux
venv\Scripts\activate         # Windows
```

### 3️⃣ Install Dependencies

```bash
uv pip install -r requirements.txt
```

### 4️⃣ Add Your API Key

Create a `.env` file in the project root:

```
ANTHROPIC_API_KEY=your_real_claude_api_key_here
```

---

## 🚀 Run the Agent

```bash
python main.py
```

You should see a structured response generated by Claude ✅

---

## ⚠️ Suppressing Warning Messages

AutoGen + Claude currently emits a cost-calculation warning.
This project already suppresses that warning automatically using:

```python
import warnings
warnings.filterwarnings("ignore", category=UserWarning)
```

So the output remains **clean and readable**.

---

## 🧩 Next Upgrade Ideas (Optional Enhancements)

| Upgrade                  | Description                                       |
| ------------------------ | ------------------------------------------------- |
| **Multi-Agent Workflow** | Add a “Code Executor” agent + Claude “Supervisor” |
| **REPL Chatbot Mode**    | Turn into a terminal chat interface               |
| **FastAPI Integration**  | Add REST endpoints for programmatic usage         |
| **RAG Mode**             | Load PDFs / Documents and answer questions        |

---

## ⭐ Project Status

| Status             | Meaning                                        |
| ------------------ | ---------------------------------------------- |
| ✅ Stable           | Basic agent working smoothly                   |
| 🧱 Ready to Extend | Designed for multi-agent + workflow automation |

---
